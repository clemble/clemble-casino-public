machine:
  java:
    version: oraclejdk8
  services:
    - docker

dependencies:
  override:
    - mvn --version
    - mvn install

deployment:
  hub:
    branch: master
    commands:
      - mvn deploy